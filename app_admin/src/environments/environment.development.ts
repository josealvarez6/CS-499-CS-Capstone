// Development environment configuration.
//
// This file contains application settings used during local development,
// keeping API endpoints centralized makes the application easier to
// maintain and allows different environments to use different configurations.

export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000/api'
};