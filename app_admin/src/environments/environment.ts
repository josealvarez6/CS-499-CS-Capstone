// Production environement configuration.
//
// This file contains the application settings used when the Angular
// application is built for productions. API endpoints are centralized
// here to avoid hard-coded values throught the application.

export const environment = {
  production: true,
  apiBaseUrl: '/api'
};